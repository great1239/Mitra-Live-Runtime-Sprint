# Runtime Cleanup

The direct companion-to-product branch and cross-product fallback behavior were
removed from user auto-dispatch. Low-level dispatch remains an internal
transport primitive, not an alternate user workflow.

