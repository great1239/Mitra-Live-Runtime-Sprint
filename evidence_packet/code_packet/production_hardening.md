# Production Hardening

Canonical execution inherits the existing checkpoint model, dependency
preflight, typed failure states, recovery endpoint, immutable stage artifacts,
PostgreSQL locking, metrics, telemetry, and depository export.

