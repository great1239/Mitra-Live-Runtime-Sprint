# Deployment Changes

No new deployment platform is introduced. The canonical runtime continues to
use Vercel for the API/dashboard and Render for independently hosted owner
services. Production secrets remain platform-managed and absent from Git.

